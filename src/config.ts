/** ケース一辺のマス数（立方体） */
export const GRID_N = 5;

/** ケース全体の 90° 回転アニメーション時間（ミリ秒） */
export const SPIN_DURATION_MS = 420;

/** ブロック移動・落下のイージング時間（ミリ秒） */
export const MOVE_DURATION_MS = 480;

/** ゲーム開始時にランダムで置く初期ボクセル数（レベル1 = 数字2） */
export const START_VOXELS = 10;
